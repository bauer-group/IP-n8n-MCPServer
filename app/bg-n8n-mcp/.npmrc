# Registry and auth only.
#
# pnpm 11 no longer reads *settings* from .npmrc — those live in
# pnpm-workspace.yaml. Keeping this file to registry configuration makes that
# split explicit, so nobody adds `node-linker=hoisted` here and spends an
# afternoon wondering why it has no effect.
registry=https://registry.npmjs.org/
